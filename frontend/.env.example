# Local dev: leave this file unused, api.js defaults to http://localhost:5000/api
# Deployment (Vercel): set this as an Environment Variable in the Vercel dashboard,
# NOT in this file — pointing to your deployed Render backend URL + /api
# Example: VITE_API_URL=https://smart-campus-backend.onrender.com/api
